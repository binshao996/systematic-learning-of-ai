import { describe, it, expect } from "bun:test";

describe("Evaluator", () => {
  it.todo("summary formats results as a string (requires service setup)");
  it.todo("runEval returns results for test cases (requires DeepSeek API)");
});
